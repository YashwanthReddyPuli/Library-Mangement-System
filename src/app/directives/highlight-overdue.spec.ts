import { HighlightOverdue } from './highlight-overdue';

describe('HighlightOverdue', () => {
  it('should create an instance', () => {
    const directive = new HighlightOverdue();
    expect(directive).toBeTruthy();
  });
});
